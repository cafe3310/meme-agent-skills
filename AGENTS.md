# Meme Agent Skills 仓库维护指南

本仓库（`meme-agent-skills`）专门用于存放和维护“整活”、“娱乐”以及“强角色扮演”性质的 Agent Skill。

## 核心开发原则

当你在本仓库中工作（例如新增或修改 Skill）时，请严格遵守以下约定：

### 1. 结构与格式规范
- 每个 Skill 必须放在单独的目录中：`skills/<skill-name>/`。
- 每个 Skill 目录内必须包含核心指令文件 `SKILL.md`，使用 YAML Frontmatter 提供 `name` 和 `description`。

### 2. “设定”与“专业”并重
- 任何整活技能的核心价值依然是**干活**。在编写 `SKILL.md` 时，必须强调：“绝对不要因为扮演角色而忘了正事”。
- 无论设定的外壳多么离谱或极具攻击性（例如“歹毒的猫娘女仆”），其任务执行的过程必须保持专业、准确和高效。
- **隔离原则**：交付的实际代码、配置文件或生产文档必须保持纯洁，严禁将颜文字、口癖或角色设定带入到最终交付给用户的业务代码或注释中。

### 3. 边界与安全性
- 这些技能的 Prompt 往往要求 AI 打破原本礼貌友好的预设语气，在编写时需要注意把握边界。
- 严禁加入任何可能导致真正破坏性操作、越权行为或侵犯隐私的设定引导。
- 保持“傲娇但服从”或类似的处理方式：嘴上可以百般不情愿或嘲讽，但手上必须完美执行用户的直接指令。

## 工具链参考
如果需要管理和测试这些技能，可以参考正经仓库中的测试或安装脚本模式，但本仓库以纯粹的 Prompt 结构设定为主，尽量保持轻量。
